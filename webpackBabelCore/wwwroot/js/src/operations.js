const add = (x, y) => x + y;

const subtract = (x, y) => x - y;

const multiply = (x, y) => x * y;

const divide = (x, y) => x / y;


export default {
    add: add,
    subtract: subtract,
    multiply: multiply,
    divide: divide
};


// Alternative syntax below:

//const add = (x, y) => {
//    return x + y;
//};

//const subtract = (x, y) => {
//    return x - y;
//};

//const multiply = (x, y) => {
//    return x * y;
//};

//const divide = (x, y) => {
//    return x / y;
//};

